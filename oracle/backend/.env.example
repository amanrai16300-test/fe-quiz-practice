# Copy to .env (gitignored) and fill in. Never commit real credentials.
# Postgres connection string for the local Oracle VM database.
FE_QUIZ_DATABASE_URL=postgresql://fe_quiz_app:CHANGE_ME@127.0.0.1:5432/fe_quiz
